---
import { Icon } from 'astro-icon'

const values = [
  {
    value: 'Partnerships with our customers',
    text: 'Whose loyalty has brought longevity and success.',
    icon: 'flame',
  },
  {
    value: 'Partnerships with our employees,',
    text: 'Whose dedication has built the company.',
    icon: 'trending-up',
  },
  {
    value: 'Belief in secure communities',
    text: 'Upholding Safety, Trust, and Prosperity',
    icon: 'users',
  },
]
---

<section class="px-4 pb-12 pt-10 sm:px-6 md:pb-16 md:pt-12 lg:px-8">
  <div class="mx-auto w-full max-w-screen-xl">
    {/* Section text */}
    <div class="mx-auto w-full max-w-xl text-center md:max-w-2xl lg:max-w-3xl">
      <p
        class="inline-flex items-center justify-center rounded-r-full rounded-tl-full bg-gradient-to-r from-dark-600 to-dark-700 px-6 py-2 text-sm font-medium tracking-wide text-white"
      >
        Our values
      </p>
      <h2
        class="mt-6 text-3xl font-extrabold text-white sm:text-4xl md:text-5xl"
      >
        The values that guide and define our work
      </h2>
      <p class="mt-6 text-xl text-dark-300">
        We pledge to do our very best to build and strengthen the partnerships which our business is based:
      </p>
    </div>

    {/* Value cards */}
    <div
      class="mt-12 grid gap-y-8 lg:mt-16 lg:grid-cols-3 lg:gap-x-6 2xl:gap-x-12"
    >
      {
        values.slice(0, 3).map((item) => (
          <div class="mx-auto w-full max-w-lg rounded-3xl bg-dark-700 px-4 py-14 shadow-xl lg:px-6 lg:py-16 xl:px-12">
            <div class="mx-auto max-w-sm text-center">
              <span class="mx-auto flex h-18 w-18 items-center justify-center rounded-3xl bg-gradient-to-r from-dark-800 to-dark-900 shadow-inner">
                <Icon
                  name={item.icon}
                  class="mx-auto h-10 w-10 text-dark-300"
                />
              </span>
              <div class="mt-8 text-2xl font-bold text-white ">
                {item.value}
              </div>
              <p class="mt-3 text-lg leading-relaxed text-dark-300">
                {item.text}
              </p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
