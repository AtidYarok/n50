---
import { Image } from 'astro:assets'
import { Icon } from 'astro-icon'

import managementImage from '@images/stock/uniform.jpg'
import handbookImage from '@images/stock/handbook.jpg'

const features = [
  {
    headline: 'Our entire employment process assists us in selecting qualified individuals who will ultimately provide the best security services available.',
    text: 'We invest considerable time and expense in our most important asset, our security personnel. Our pre-employment process is extensive. Verification of state issued permits, and a thorough background check with criminal and civil index checks as well as interviews with former employers are performed. Upon passing our stringent screening process, new staff members are required to undergo advanced training in all areas related to, or specific to, their assigned duties to ensure smooth communications and client satisfaction. Additional training is provided by legal experts and Law Enforcement Trainers in the field of civil and criminal laws. ',
    text2: 'Our employees are continually monitored, updated and trained in new laws and regulations. We believe customer service and satisfaction results from personnel that are professional and skilled in maintaining authority in the field while always exercising tact and diplomacy.',
    text3: '',
    text4: '',
    list: [],
    image: {
      src: managementImage,
      alt: 'Management',
    },
    items: [
      {
        label: '',
        text: '',
        icon: '',
      },
    ],
  },
]
---

<section class="bg-dark-800 px-4 pb-10 sm:px-6 md:pb-16 md:pt-12 lg:px-8">
  {/* Features container */}
  <div class="mx-auto w-full max-w-screen-xl">
    {
      features.map((feature, i) => (
        <div
          class:list={[
            'mx-auto max-w-xl md:max-w-2xl lg:mx-0 lg:grid lg:max-w-none lg:grid-cols-3 lg:gap-x-16 xl:gap-x-20 2xl:gap-x-24',
            { 'mt-24 lg:mt-32': i % 2 === 1 },
          ]}
        >
          {/* Feature text with icons */}
          <div class:list={[{ 'lg:order-2': i % 2 === 1 }, 'col-span-2']}>
            <div class="text-center lg:text-left">
              
              <h2 class="mt-4 text-3xl font-extrabold text-white sm:text-4xl md:text-5xl lg:mt-6">
                {feature.headline}
              </h2>
              <p class="mt-4 text-xl text-dark-300 lg:mt-6">{feature.text}</p>
              <p class="mt-4 text-xl text-dark-300 lg:mt-6">{feature.text2}</p>
              <div class="mx-auto mt-8 flex max-w-xs flex-col space-y-8 sm:mx-0 sm:mt-10 sm:max-w-none sm:flex-row sm:space-x-4 sm:space-y-0 lg:space-x-6">
                {feature.items.slice(0, 3).map((item) => (
                  <div class="flex flex-col items-center lg:items-start">
                    <div class="flex flex-col items-center sm:flex-row">
                      
                      <div class="mt-3 text-xl font-semibold text-white sm:mt-0">
                        {item.label}
                      </div>
                    </div>
                    <p class="mt-1 text-lg leading-relaxed text-dark-300 sm:mt-3">
                      {item.text}
                    </p>
                  </div>
                ))}
                <div class="mt-6 space-y-2 lg:mt-8">
                 
            </div>
              </div>
            </div>
          </div>
          {/* Feature image */}
          <div class="col-span-1 mt-10 flex items-center lg:order-1 lg:mt-0">
            <div class="aspect-h-3 aspect-w-2 relative w-full rounded-3xl text-center shadow-xl">
              <Image
                class="absolute inset-0 h-full w-full rounded-3xl object-cover object-center align-middle"
                src={feature.image.src}
                alt={feature.image.alt}
              />
            </div>
          </div>
        </div>
      ))
    }
  </div>
</section>
